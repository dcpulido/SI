// Implementation of the example described in chapter 2
// of the Jason's manual

MAS queens {

    infrastructure: Centralised

    environment: QueensEnv
    
    agents: //r2; r1; r0; 
        //white r0.asl [beliefs="playAs(0)"];

        //black r0.asl [beliefs="playAs(1)"];
		
        //block r0.asl;

        white configurer.asl [beliefs="playAs(0)"];

        black configurer.asl [beliefs="playAs(1)"];

        configurer;

}
